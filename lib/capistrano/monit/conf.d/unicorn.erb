check process <%= fetch(:application) %>_unicorn with pidfile <%= shared_path %>/pids/unicorn.pid
  start program = "/etc/init.d/unicorn start" as uid deploy
  stop program = "/etc/init.d/unicorn stop" as uid deploy
  if 5 restarts within 5 cycles then timeout
